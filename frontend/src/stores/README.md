# Stores

If needed, create your Pinia stores here to manage application state.

Example store structure:
```typescript
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useReferralStore = defineStore('referral', () => {
  // Your state, getters, and actions here
  return {}
})
```
